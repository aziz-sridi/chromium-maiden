{
  "name": "chromium-maiden",
  "version": "1.1.0",
  "private": true,
  "description": "Development checks for the Chromium Maiden extension.",
  "scripts": {
    "test": "node --test tests/moderationQueue.test.js",
    "check:js": "node --check extension/background.js && node --check extension/contentScript.js && node --check extension/popup.js && node --check extension/utils/apiClient.js && node --check extension/utils/domDetection.js && node --check extension/utils/moderationQueue.js"
  }
}
